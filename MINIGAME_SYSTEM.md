# MINIGAME SYSTEM — 공통 제작 가이드

> 이 문서는 어떤 소재의 미니게임을 만들더라도 동일한 분위기와 사용감을 유지하기 위한 공통 규칙이다.  
> 새 게임을 만들 때마다 프로젝트 루트에 이 파일을 넣고, Claude Code가 구현 전에 반드시 먼저 읽도록 한다.

---

## 0. 핵심 정의

우리가 만드는 게임은 **“단순한데 이상하게 완성도 높은 인터넷 미니게임”**이어야 한다.

예쁜 앱, SaaS, 브랜드 랜딩페이지처럼 만들지 않는다.

핵심은 다음 5가지다.

1. **설명 없이 바로 이해되는 행동 1개**
2. **실제 물체를 만지는 듯한 인터랙션**
3. **한 번의 시도로 결과가 결정되는 긴장감**
4. **결과를 점수/등급으로 평가**
5. **결과를 저장·공유하고 친구와 비교하고 싶은 구조**

예시:
- 치약 똥 만들기 → 꾸욱 눌러서 짜고 모양 만들기
- 보호필름 붙이기 → 위치 맞추고 한 번에 밀착시키기
- 스티커 붙이기 → 기울기/위치/기포 없이 붙이기
- 계란 깨기 → 힘 조절해서 껍질만 깨기
- 케이크 크림 짜기 → 일정한 압력으로 모양 만들기
- 주차하기 → 한 번에 선 안에 정확히 넣기

---

# 1. 디자인 철학

## 1-1. 전체 무드

다음 키워드를 유지한다.

- simple
- plain
- black and white
- raw
- direct
- slightly silly
- functional
- tactile
- old-school web toy
- visually clean, mechanically satisfying

사용자가 첫 화면을 보고 느껴야 하는 감정:

> “뭐야 이거 ㅋㅋ 바로 해봐야겠다”

---

## 1-2. 절대 만들지 말 것

다음 방향은 금지한다.

- 스타트업 랜딩페이지
- SaaS 대시보드
- 파스텔 모바일 앱
- 감성 브랜딩 페이지
- 큰 둥근 카드
- glassmorphism
- gradient CTA
- 과한 shadow
- 컬러풀한 UI
- 기능보다 장식이 많은 화면
- 설명이 긴 튜토리얼
- 캐릭터가 말을 많이 하는 구조
- 버튼/카드가 지나치게 예쁜 디자인

**게임 오브젝트가 컬러를 담당하고, UI는 거의 흑백으로 유지한다.**

---

# 2. 공통 컬러 시스템

## UI

```css
--bg: #FFFFFF;
--text: #000000;
--muted: #777777;
--border: #000000;
--soft-border: #DDDDDD;
--disabled: #E5E5E5;
```

Primary Button:

```css
background: #000000;
color: #FFFFFF;
border: 1px solid #000000;
```

Secondary Button:

```css
background: #FFFFFF;
color: #000000;
border: 1px solid #000000;
```

### 규칙

- UI 포인트 컬러 사용 금지
- 오브젝트 고유 색상은 허용
- 결과물 자체는 컬러 유지
- 점수/버튼/게이지는 흑백 유지
- 성공/실패를 초록/빨강으로 과하게 표시하지 않는다

---

# 3. 형태 규칙

## Border Radius

기본:

```text
0px ~ 4px
```

큰 둥근 카드 금지.

## Shadow

UI 요소에 큰 그림자 금지.

허용:
- 실제 물체의 접촉 그림자
- 사진 속 자연스러운 그림자
- 오브젝트의 물리적 그림자

## Border

필요할 경우:

```text
1px solid #000
```

또는 아주 옅은 회색 구분선.

---

# 4. Typography

한글 가독성이 좋은 sans-serif 사용.

추천:
- Pretendard
- system-ui
- Apple SD Gothic Neo
- Noto Sans KR

### 기본 위계

메인 제목:
- 27~32px
- 800~900

섹션 제목:
- 20~24px
- 700~800

본문:
- 14~16px
- 400~600

점수:
- 48~64px
- 800~900

버튼:
- 15~17px
- 700~800

장식용 영문 폰트 사용 금지.

---

# 5. 모바일 우선

모든 게임은 모바일 중심으로 설계한다.

기준 너비:

```text
320
360
375
390
430
```

PC에서는 모바일 게임 영역만 가운데 배치한다.

```css
.app {
  width: 100%;
  max-width: 430px;
  margin: 0 auto;
}
```

PC 화면이라고 가로 레이아웃으로 확장하지 않는다.

---

# 6. 공통 화면 구조

모든 게임은 가능하면 아래 5단계 구조를 유지한다.

```text
INTRO
↓
SELECT / READY
↓
TUTORIAL
↓
PLAY
↓
RESULT
```

필요한 경우:

```text
CHALLENGE
BATTLE RESULT
```

를 추가한다.

---

# 7. INTRO 화면

첫 화면은 최대한 단순해야 한다.

기본 구조:

```text
[대표 이미지 또는 게임 오브젝트]

메인 카피

[시작하기]
```

### 규칙

- 서브카피는 없어도 된다
- 기능 설명 넣지 않는다
- 이모지 남발 금지
- 버튼은 1개
- 대표 이미지가 게임의 행동을 바로 암시해야 한다

### 메인 카피 문법

짧고 직접적으로 쓴다.

예:
- 완벽한 치약 똥을 만드세요
- 먼지 없이 보호필름을 붙이세요
- 한 번에 정확히 주차하세요
- 계란을 깨뜨리지 말고 까보세요
- 스티커를 딱 맞게 붙이세요

**명령형 + 목표가 명확한 문장**을 우선한다.

---

# 8. SELECT / READY 화면

게임에 재료/난이도/색상 선택이 필요한 경우에만 넣는다.

선택 UI는 리스트형으로 단순화한다.

예:

```text
○ 기본 필름
○ 무광 필름
○ 프라이버시 필름
```

선택 항목 전체를 컬러 카드로 만들지 않는다.

필요하면 작은 컬러 점 또는 작은 샘플만 보여준다.

---

# 9. TUTORIAL 화면

튜토리얼은 **3단계 이내**로 끝낸다.

기본 구조:

```text
1. 누르기
2. 움직이기
3. 떼기
```

또는 게임에 맞게:

```text
1. 위치 맞추기
2. 천천히 내리기
3. 한 번에 밀착
```

### 금지

- 긴 설명문
- 여러 장의 튜토리얼 슬라이드
- 둥근 카드 + 번호 배지
- 튜토리얼만 10초 이상 걸리는 구조

가능하면 한 화면 안에 끝낸다.

---

# 10. PLAY 화면

플레이 화면은 **게임 오브젝트가 80%, UI가 20%**여야 한다.

우선순위:

1. 실제 조작 대상
2. 결과가 생기는 표면/공간
3. 입력 피드백
4. 최소한의 게이지
5. 나머지 UI

### 플레이 중 UI는 최소화

허용:
- 남은 시간
- 남은 재료
- 진행률
- 아주 짧은 안내

금지:
- 여러 개의 상태 카드
- 설명 패널
- 점수 실시간 노출
- 너무 많은 버튼

---

# 11. 실제 물체처럼 느껴지게 만들기

각 게임의 핵심은 **“실제 행동의 물리적 인상”**을 살리는 것이다.

게임을 구현하기 전에 반드시 아래 질문에 답한다.

### 물체 질문

1. 현실에서는 무엇을 누르는가?
2. 무엇이 움직이는가?
3. 어떤 힘이 작용하는가?
4. 마찰/점성/저항/탄성 중 무엇이 중요한가?
5. 실패할 때 실제로 어떤 현상이 생기는가?
6. 성공했을 때 어떤 시각적 만족이 생기는가?

### 예: 보호필름 붙이기

- 필름은 완벽한 직사각형이지만 살짝 휘어진다
- 가장자리부터 접촉한다
- 눌러붙는 순간 접착 영역이 퍼져나간다
- 먼지가 있으면 작은 원형 기포가 생긴다
- 너무 빨리 붙이면 큰 기포가 갇힌다
- 중심이 틀어지면 베젤과 평행하지 않다
- 마지막에 기포가 쫙 빠지면 시각적으로 만족스럽다

---

# 12. “물리 시뮬레이션”보다 “체감” 우선

정확한 물리학보다 **시각적으로 그럴듯한 pseudo physics**를 우선한다.

목표:

> 실제와 완전히 같지는 않아도, 사용자가 “아 이 느낌 알지”라고 느끼게 한다.

따라서 필요하면:
- 자동 보정
- 제한된 관성
- 시각적 보간
- 위치 스냅
- 과장된 압축
- 짧은 지연

등을 사용한다.

단, 사용자의 입력을 무시하고 결과를 자동 생성하면 안 된다.

---

# 13. 입력 규칙

PC와 모바일을 따로 구현하지 않는다.

Pointer Events 사용.

```text
pointerdown
pointermove
pointerup
pointercancel
```

Canvas / game stage에만:

```css
touch-action: none;
```

을 적용한다.

---

# 14. 한 번의 시도가 중요해야 한다

가능하면 게임은:

```text
시작
→ 조작
→ 손을 떼거나 끝나는 순간 종료
```

구조로 만든다.

중간에 무한 수정할 수 있게 하지 않는다.

이 구조가:

- 긴장감
- 재도전
- 친구 비교
- 점수 자랑

을 만든다.

---

# 15. 입력이 결과에 실제로 반영되어야 한다

아래 값들을 게임별로 기록한다.

예:

```text
pointer path
speed
acceleration
hold time
direction changes
center offset
angle
pressure proxy
release velocity
completion time
error count
```

Math.random()으로 결과 점수 생성 금지.

---

# 16. “예쁘게 자동 보정” 금지

사용자가 못했는데 결과만 예쁘게 바꿔주지 않는다.

허용:
- 조작 가능한 범위 안에서 약한 assist
- 물리적으로 말이 되는 smoothing
- 화면 밖 이탈 방지
- 미세한 중심 attraction

금지:
- 플레이 종료 후 완성 이미지를 교체
- 미리 만든 성공 이미지를 사용
- 점수를 결과와 무관하게 생성

---

# 17. 결과 화면

공통 구조:

```text
[완성 결과]

XX점

결과 등급

짧은 평가 문구

세부 점수

[다시 하기]

[친구에게 도전장 보내기]

[이미지 저장] [공유하기]
```

결과 화면도 흑백 UI 유지.

결과물만 컬러.

---

# 18. 점수 구조

총점은 기본 100점.

4~6개 항목으로 나눈다.

예시:

```text
정확도      25
균형        20
완성도      20
속도        15
실수        10
마무리      10
```

게임마다 의미 있게 바꾼다.

### 보호필름 붙이기 예시

```text
정렬        25
기포        25
먼지        20
밀착        15
속도        10
마무리       5
```

---

# 19. 평가 문구 톤

짧고 웃겨야 한다.

과도한 밈 문장보다 **무심하게 웃긴 톤**이 좋다.

예:

95~100
- 이건 그냥 맡겨도 되겠습니다.
- 돈 받고 붙여도 될 수준.

80~94
- 제법 합니다.
- 한두 군데만 아쉽습니다.

60~79
- 사용할 수는 있습니다.
- 신경 쓰이지만 참을 만합니다.

40~59
- 다시 붙이는 게 낫겠습니다.
- 기포가 존재감을 주장합니다.

0~39
- 필름 한 장 더 준비하세요.
- 처음부터 다시 갑시다.

---

# 20. 저장 이미지

결과를 SNS에 올릴 수 있게 만든다.

권장:
- 1080 × 1920
- 흰 배경
- 검은 텍스트
- 결과 오브젝트만 컬러

포함:

```text
게임명
결과 이미지
점수
등급
짧은 문구
```

과한 템플릿 디자인 금지.

---

# 21. 공유

지원되는 모바일에서는 Web Share API 사용.

지원되지 않을 경우 이미지 저장 fallback.

특정 SNS 앱 실행에 강하게 의존하지 않는다.

---

# 22. 친구 도전

가능하면 모든 게임에 “도전장” 구조를 넣는다.

```text
내 기록
92점

이길 수 있으면 해보세요.

[도전 시작]
```

도전 전에는 상대 결과 디테일을 숨겨도 된다.

결과 후:

```text
A 92
VS
B 87
```

단순하게 비교.

---

# 23. 게임별 성공 기준 정의

각 게임마다 개발 전에 성공 형태를 정확히 정의한다.

예: 보호필름

완벽한 결과:
- 화면 중심 정렬
- 상하좌우 여백 일정
- 먼지 0
- 큰 기포 0
- 미세 기포 0
- 주름 0
- 한 번에 부착
- 마지막 밀착이 자연스러움

실패 형태:
- 비뚤어짐
- 가장자리 들뜸
- 먼지
- 기포
- 접힘
- 중간에 다시 뗌

---

# 24. 반드시 “실패 비주얼”을 먼저 정의한다

Claude Code에게 좋은 결과만 설명하지 않는다.

게임별로 반드시:

```text
이렇게 나오면 실패
```

를 명시한다.

예: 보호필름

금지:
- 필름이 종이처럼 딱딱함
- 화면 위를 그냥 사각형이 따라다님
- 붙이는 순간 전체가 동시에 접착됨
- 기포가 랜덤 원 몇 개로 생성됨
- 먼지와 기포의 관계가 없음
- 위치가 틀려도 자동으로 정렬됨

---

# 25. 에셋 사용 원칙

실사 오브젝트가 중요하면:
**CSS/SVG로 억지로 다시 만들지 말고 실제 에셋을 사용한다.**

예:
- 휴대폰
- 칫솔
- 접시
- 계란
- 유리
- 포장지
- 손
- 도구

프로젝트의 assets 폴더를 먼저 확인한다.

에셋이 있다면 그것을 우선 사용한다.

Claude가 임의의 허접한 CSS 그림을 새로 만들지 않는다.

---

# 26. 게임 오브젝트와 UI 분리

항상:

```text
UI = 흑백, 단순
GAME OBJECT = 실제 컬러/재질
```

로 분리한다.

예:
- 보호필름: 투명/반사/기포 표현 가능
- 스마트폰: 실제 컬러
- UI 버튼: 검정/흰색

---

# 27. 애니메이션 원칙

애니메이션은 장식이 아니라 **입력에 대한 물리 피드백**으로 사용한다.

좋은 애니메이션:
- 누르면 눌림
- 당기면 늘어남
- 붙이면 접착 영역 퍼짐
- 놓으면 반동
- 실패하면 들뜸
- 기포가 밀려나감

나쁜 애니메이션:
- 의미 없는 bounce
- 버튼 반짝임
- 계속 떠다니는 장식
- 배경 파티클

---

# 28. 시간감

입력과 결과 사이에 아주 작은 지연이 있으면 실제처럼 느껴진다.

예:
- 누름 → 100ms 후 압출
- 필름 접촉 → 80ms 후 접착 확산
- 힘을 뗌 → 150ms 후 원상복구

단, 조작이 답답해질 정도로 늦으면 안 된다.

---

# 29. 결과 settling

플레이 종료 직후 결과를 바로 띄우지 않는다.

약 0.5~1.2초 동안:
- 물체가 안정화
- 기포가 멈춤
- 필름이 붙음
- 크림이 흔들림
- 오브젝트가 정착

후 점수 공개.

이 짧은 정적 시간이 결과의 “완성감”을 만든다.

---

# 30. 사운드

사운드를 넣는 경우 아주 짧고 기능적으로 사용한다.

예:
- 눌림
- 붙음
- 기포 터짐
- 완료

BGM 필수 아님.

음소거 상태에서도 게임이 완전히 이해되어야 한다.

---

# 31. 상태 구조

권장:

```js
const STATE = {
  INTRO,
  SELECT,
  TUTORIAL,
  PLAYING,
  SETTLING,
  RESULT,
  CHALLENGE,
  BATTLE_RESULT
};
```

상태마다 이벤트 리스너를 중복 생성하지 않는다.

---

# 32. 프로젝트 구조 추천

```text
/
├─ index.html
├─ src/
│  ├─ main.js
│  ├─ game/
│  │  ├─ state.js
│  │  ├─ input.js
│  │  ├─ physics.js
│  │  ├─ renderer.js
│  │  ├─ scoring.js
│  │  └─ config.js
│  └─ styles/
│     └─ main.css
│
├─ assets/
│  ├─ ...
│
└─ MINIGAME_SYSTEM.md
```

작은 게임이면 구조를 더 단순화해도 된다.

---

# 33. 개발 순서

반드시 아래 순서로 진행한다.

## PHASE 1
핵심 조작만 구현

## PHASE 2
물리적 체감 구현

## PHASE 3
실패/성공 상태 구현

## PHASE 4
점수 계산

## PHASE 5
Intro / Tutorial / Result UI

## PHASE 6
저장 / 공유

## PHASE 7
친구 도전

**UI보다 게임 손맛이 먼저다.**

---

# 34. QA 체크

게임 완성 후 반드시 확인한다.

### 시각

- [ ] 앱처럼 과하게 예쁘지 않은가?
- [ ] UI가 거의 흑백인가?
- [ ] 게임 오브젝트가 가장 먼저 보이는가?
- [ ] 큰 둥근 카드가 없는가?
- [ ] 의미 없는 gradient/shadow가 없는가?

### 조작

- [ ] 누르면 즉각 피드백이 있는가?
- [ ] 움직임에 관성/저항이 적절한가?
- [ ] 실제 행동과 비슷한가?
- [ ] 모바일에서 손가락에 가리지 않는가?
- [ ] 320px에서도 플레이 가능한가?

### 결과

- [ ] 입력에 따라 실제 결과가 달라지는가?
- [ ] 점수가 random이 아닌가?
- [ ] 잘한 결과와 못한 결과가 시각적으로 구분되는가?
- [ ] 다시 하고 싶은가?
- [ ] 친구에게 보내고 싶은가?

---

# 35. 게임별 명세를 추가하는 위치

새 게임을 만들 때 이 문서 아래에
`GAME SPEC`만 추가하면 된다.

---

# GAME SPEC TEMPLATE

아래 내용을 복사해서 게임마다 작성한다.

```md
# GAME SPEC

## 게임명
[게임명]

## 한 줄 목표
[사용자가 무엇을 한 번에 성공해야 하는가]

## 메인 카피
[짧은 명령형 문장]

## 핵심 행동
[누르기 / 드래그 / 기울이기 / 놓기 등]

## 실제 행동의 느낌
[현실에서 어떤 저항, 압력, 마찰, 탄성 등이 있는지]

## 성공 상태
- 
- 
- 

## 실패 상태
- 
- 
- 

## 입력값
- pointer path
- speed
- angle
- hold time
- release speed
- 기타

## 평가 항목 / 100점
- 항목 1:
- 항목 2:
- 항목 3:
- 항목 4:
- 항목 5:

## 필요한 에셋
- 
- 
- 

## 반드시 구현해야 할 물리적 피드백
- 
- 
- 

## 절대 나오면 안 되는 실패 표현
- 
- 
- 

## 결과 등급
95~100:
80~94:
60~79:
40~59:
0~39:

## 공유 문구
[결과 이미지에 들어갈 한 줄]
```

---

# 36. 예시 — 보호필름 붙이기

```md
# GAME SPEC

## 게임명
보호필름 붙이기

## 한 줄 목표
먼지와 기포 없이 보호필름을 한 번에 정확하게 붙인다.

## 메인 카피
완벽하게 보호필름을 붙이세요

## 핵심 행동
필름 위치를 맞춘 뒤,
상단 또는 한쪽 가장자리부터 화면에 천천히 내려놓는다.

필름이 접촉하기 시작하면
사용자는 손가락으로 접착 영역을 밀어내며 붙인다.

한 번 완전히 붙인 뒤 다시 떼는 것은 불가능하다.

## 실제 행동의 느낌

필름은 얇고 살짝 휘어진다.

화면에 닿으면:
접착 영역이 천천히 퍼진다.

빠르게 눌러 붙이면:
공기가 빠져나가지 못하고 기포가 생긴다.

먼지 위에 붙으면:
해당 위치에 작은 원형 기포가 영구적으로 남는다.

필름을 비뚤게 시작하면:
전체 정렬이 틀어진다.

## 성공 상태

- 베젤과 평행
- 상하좌우 여백 균일
- 큰 기포 없음
- 미세 기포 없음
- 먼지 없음
- 가장자리 들뜸 없음
- 주름 없음
- 한 번에 부착

## 실패 상태

- 기울어짐
- 좌우 한쪽으로 치우침
- 먼지
- 큰 기포
- 작은 기포 다수
- 가장자리 들뜸
- 접힘
- 너무 빠르게 붙임

## 평가 항목 / 100점

정렬 25
기포 25
먼지 20
밀착 15
속도 10
마무리 5

## 반드시 구현해야 할 물리

1. 필름이 완벽하게 딱딱하지 않고 아주 살짝 휜다.
2. 화면과 접촉한 부분부터 접착 wave가 퍼진다.
3. 접착 wave 앞쪽의 공기가 가장자리로 빠져나간다.
4. 너무 빠르게 눌러버리면 공기가 갇혀 bubble이 된다.
5. 먼지가 있는 좌표에서는 공기가 빠지지 않고 작은 bubble이 남는다.
6. 기포를 손가락으로 밀면 가장자리 방향으로 이동할 수 있다.
7. 이미 접착된 영역을 과하게 들어올려 다시 정렬하지 못하게 한다.

## 절대 금지

- 필름이 단순 rectangle div처럼 움직이는 것
- 붙이는 순간 전체 화면에 동시에 snap되는 것
- 기포가 랜덤으로 아무 이유 없이 생성되는 것
- 위치가 틀렸는데 자동 중앙 정렬되는 것
- 필름이 종이처럼 딱딱한 것
- bubble이 단순한 파란 원 아이콘처럼 보이는 것

## 결과 등급

95~100:
필름 장인

80~94:
제법 잘 붙임

60~79:
조금 신경 쓰임

40~59:
기포가 존재감을 주장함

0~39:
한 장 더 사세요
```

---

# 37. 공통 화면 레이아웃 시스템 (AppShell)

> 이 섹션은 "주차하기" 게임을 실제로 만들면서 여러 차례 피드백을 거쳐 정착한 화면 배치
> 규칙이다. 이후 만드는 모든 미니게임은 처음부터 이 시스템으로 만든다 — 다 만들고 나서
> "화면마다 규격을 맞춰달라"는 요청을 다시 받지 않기 위한 섹션이다.

## 37-1. 공통 프레임 + 디자인 토큰

화면마다 width·margin·preview 크기를 따로 잡지 않는다. CSS 최상단에 토큰을 선언하고
모든 화면이 그 값만 쓴다.

```css
:root {
  --app-max-width: 430px;      /* 앱 전체 프레임 */
  --content-width: 320px;      /* 선택 리스트·CTA·본문이 공유하는 폭 */
  --preview-width: var(--content-width);
  --preview-height: 200px;     /* "설명/선택" 화면이 전부 공유하는 비주얼 프레임 하나 */

  --screen-top: 32px;          /* 화면 상단 → 비주얼 슬롯 */
  --gap-preview-title: 22px;   /* 비주얼 슬롯 → 타이틀 */
  --gap-title-content: 20px;   /* 타이틀 → 본문 */
  --gap-content-cta: 28px;     /* 마지막 콘텐츠 → CTA */
  --screen-bottom: 28px;       /* CTA → 화면 하단 */

  --button-height: 48px;
  --button-height-secondary: 44px;
}
```

세로 640px 이하 화면(예: 320×568)에서는 `@media (max-height:640px)`로 같은 변수를
비례 축소한 값으로 재정의한다(예: preview 150px, 간격 12~20px, 버튼 42/40px). 새 값을
따로 만들지 않고 토큰만 갈아끼운다 — 그래야 "짧은 화면에서 한 화면에 다 들어오는지"를
화면 하나하나 손볼 필요가 없다.

비주얼 프레임을 170×106 같은 작은 아이콘 크기가 아니라 `--content-width`와 같은
320×200(가로형)으로 잡는 이유: 결과·인트로·도전장·대결처럼 "오브젝트 여러 개가 있는
장면"을 보여줘야 하는 화면이 반드시 있고, 그 화면과 "오브젝트 1개만 보여주는" 선택
화면이 다른 프레임 크기를 쓰면 화면을 넘길 때 프레임 자체가 커졌다 작아졌다 한다.
**"동일한 값 하나를 정해서 전 화면에 쓴다"**를 지키려면, 가장 큰 콘텐츠(장면형)를
기준으로 프레임 크기를 정하고 나머지 화면(오브젝트 1개)은 그 프레임 안에서 작게
보여주는 쪽이, 그 반대(작은 프레임에 장면을 욱여넣는 것)보다 항상 쉽다.

## 37-2. 화면을 두 그룹으로 나눈다

같은 프로젝트 안의 화면이라도 **"보여주는 화면"과 "고르는 화면"은 배치 방식이 다르다.**
이 둘을 하나의 규칙으로 억지로 통일하려 하지 말고, 처음부터 두 그룹으로 나눠서
설계한다.

**A. 고르는 화면** (선택 리스트/그리드가 있는 화면 — 차종 선택, 색상 선택, 난이도 선택 등)

```text
화면 상단(--screen-top)
[비주얼 프레임]
  ↓ --gap-preview-title
[타이틀]
  ↓ --gap-title-content
[선택 리스트/그리드]   ← 내용만큼만 차지, 억지로 늘리거나 줄이지 않음
  ↓ --gap-content-cta
[CTA]
화면 하단(--screen-bottom)
```

옵션 개수가 화면마다 달라(예: 2×2 그리드 vs 4줄 리스트) 본문 높이가 원래 다르면,
**두 화면 중 더 큰 본문 높이를 기준으로 작은 쪽에도 같은 `min-height`를 주고 그 안에서
세로 중앙 정렬**한다. 이렇게 해야 사용자가 이 화면들을 연달아 넘길 때 CTA 버튼이 같은
y좌표에 고정되고, "버튼이 화면마다 왔다갔다한다"는 인상이 없다. 본문 콘텐츠 자체를
늘리거나(빈 옵션 추가 등) 줄이지 않는다 — 컨테이너 높이만 맞춘다.

**B. 보여주는 화면** (본문 리스트 없이 비주얼+타이틀+CTA뿐인 화면 — 인트로, 튜토리얼,
로딩, 안내 화면 등)

이 화면들은 위 A그룹과 똑같이 화면 상단에 고정하면 첫 화면치고 밋밋하고, 전체를 정확히
50:50으로 가운데 정렬하면 무게중심이 아래로 처져 보인다(실제 사용자 피드백). 대신:

```css
#screen-info-a, #screen-info-b {           /* 같은 그룹의 모든 화면에 적용 */
  justify-content: flex-start;
}
#screen-info-a::before, #screen-info-b::before { content: ""; flex: 1 0 0; }
#screen-info-a::after,  #screen-info-b::after  { content: ""; flex: 2 0 0; }
```

빈 `::before`/`::after`를 위아래 스페이서로 두고 flex-grow를 1:2로 주면, [비주얼+타이틀
+CTA] 묶음 전체가 화면 정중앙보다 살짝 위쪽에 뜬다. 비주얼·타이틀·CTA 사이 간격은
새로 만들지 않고 37-1의 토큰을 그대로 쓴다 — 달라지는 건 이 묶음의 화면 내 위치뿐이다.

이 그룹에 속한 화면이 2개 이상이면(예: 인트로 + 튜토리얼), **반드시 정확히 같은
y좌표에 오게 만든다.** 제목 폰트 크기가 화면마다 다르면(예: 인트로만 큰 메인 카피)
그 차이만큼 아래 요소가 밀리므로, 타이틀 요소에 공통 `min-height`를 줘서 실제 텍스트
높이와 무관하게 같은 공간을 예약하게 한다.

## 37-3. CTA는 절대 바닥에 고정하지 않는다

**금지: `position:fixed`, `margin-top:auto`, `justify-content:space-between`,
`height:100vh` 안에서 위아래를 강제로 분리하는 모든 방법.**

이렇게 만들면 화면마다 콘텐츠 밀도가 달라도 버튼만 항상 화면 맨 아래에 못 박히는데,
그 결과 콘텐츠와 버튼 사이에 화면마다 크기가 다른 어색한 빈 공간이 생기고, "이거 다음
화면 맞아?" 싶은 단절감이 생긴다. CTA는 **항상 "마지막 콘텐츠 바로 아래"**(37-2-A)
또는 **"화면 안에서 콘텐츠 묶음과 함께 이동"**(37-2-B) 둘 중 하나여야 한다.

## 37-4. 오브젝트 미리보기는 고정 scale을 쓰지 않는다

같은 오브젝트를 크기가 다른 여러 종류로 보여줘야 하는 게임(차종 4종, 캐릭터 여러 종
등)에서, 미리보기를 고정 배율로 그리면 원래 작은 오브젝트는 프레임 안에서 지나치게
작아 보이고 큰 오브젝트만 꽉 차 보인다. 실측으로 확인된 문제다(프레임 채움 비율이
41%~88%까지 벌어짐). 고정 scale 대신 프레임에 padding만 주고 **매번 프레임에 맞춰
자동으로 배율을 계산**하게 한다 — 그래야 크기 차이가 "실제 크기 차이만큼만" 보이고,
"같은 카드인데 왜 크기가 다르지" 하는 인상이 생기지 않는다.

## 37-5. 화면 전환

슬라이드 없이 아주 짧은 fade만 쓴다: opacity 0→1 + translateY(4px→0), 150~180ms.
레이아웃 위치 자체는 전환 애니메이션과 무관하게 절대 바뀌지 않는다 — 애니메이션은
연속성을 "느끼게" 하는 장치이지, 위치가 어긋난 걸 가려주는 장치가 아니다.

## 37-6. "정확히 같은 위치" 요청은 반드시 실측으로 검증한다

사용자가 "버튼 위치 고정", "위치 맞춰줘" 같은 요청을 하면, 스크린샷을 눈으로 비교하는
것만으로 끝내지 않는다. 헤드리스 브라우저로 두 화면의 해당 요소를
`getBoundingClientRect()`로 찍어서 y좌표 오차가 0에 가까운지 직접 확인한다. 타이틀
폰트 크기 차이처럼 몇 px 단위 오차는 스크린샷만으론 안 보이고, 다음 피드백("더
올려줘", "그것도 맞춰줘")으로 이어진다.

## 37-7. 새 게임을 시작할 때 바로 쓸 수 있는 요청 문장

이 시스템을 처음부터 적용하고 싶으면, 게임별 GAME SPEC과 함께 아래 문장을 첫
프롬프트에 넣는다(38번 템플릿에 이미 포함되어 있다):

```text
화면 레이아웃은 MINIGAME_SYSTEM.md 37번 섹션(공통 화면 레이아웃 시스템)을 그대로 따라.

- 모든 화면이 공유하는 디자인 토큰(app-max-width, content-width, preview 크기,
  화면 상단/요소 간 간격, 버튼 높이)을 먼저 정의하고 화면마다 새로 만들지 마.
- 화면을 "고르는 화면"(선택 리스트/그리드)과 "보여주는 화면"(인트로/튜토리얼 등
  본문 없는 화면)으로 나눠. 고르는 화면끼리는 본문 높이를 통일해서 CTA가 화면을
  넘겨도 같은 y에 고정되게 하고, 보여주는 화면끼리는 위:아래 1:2 비율로 정중앙보다
  살짝 위에 묶음 전체를 배치해.
- CTA는 항상 콘텐츠 바로 아래. position:fixed·margin-top:auto·space-between으로
  바닥에 고정하지 마.
- 오브젝트 미리보기는 고정 scale 금지, 프레임에 맞춰 자동으로 배율 계산.
- 다 만들고 나서 헤드리스 브라우저로 각 화면의 비주얼/타이틀/CTA 좌표를 실측해서
  같은 그룹끼리 정확히 같은 y에 오는지 확인해.
```

---

# 38. Claude Code에 처음 넣을 공통 명령

새 게임을 만들 때 첫 프롬프트는 아래처럼 짧게 시작한다.

```text
프로젝트 루트의 MINIGAME_SYSTEM.md를 먼저 처음부터 끝까지 읽어줘.

이 프로젝트는 해당 문서의 디자인 시스템, 게임 UX, 물리 표현, 점수 구조,
모바일 우선 원칙을 반드시 따라야 한다.

특히 화면 배치는 37번 섹션(공통 화면 레이아웃 시스템)의 토큰·규칙을 처음부터
그대로 적용해. 화면마다 규격을 다르게 잡았다가 나중에 통일하는 식으로 하지 마.

이번 게임의 GAME SPEC은 아래와 같다.

[여기에 게임별 GAME SPEC 붙여넣기]

먼저 핵심 게임 인터랙션과 물리부터 구현해.
UI를 먼저 꾸미지 말고,
실제 조작감 → 성공/실패 상태 → 점수 → UI 순서로 작업해.

MINIGAME_SYSTEM.md와 충돌하는 임의의 디자인 해석은 하지 마.
```

---

# 39. 최종 원칙

이 게임 시리즈는 디자인보다 **행동의 촉감**이 브랜드다.

게임 소재가 무엇이든:

- 눌렀을 때
- 움직였을 때
- 놓았을 때
- 실패했을 때
- 성공했을 때

그 행동의 느낌이 실제처럼 전달되어야 한다.

그리고 모든 게임이 다음 구조를 공유해야 한다.

```text
단순한 첫 화면
→ 한 문장 목표
→ 짧은 튜토리얼
→ 한 번의 집중 플레이
→ 결과 평가
→ 다시 도전
→ 저장 / 공유
```

게임마다 비주얼은 달라도
이 감각은 반드시 동일하게 유지한다.
